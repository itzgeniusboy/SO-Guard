build:
  compression: zstd
  compression_level: 19
  min_api_level: 21
  protection_level: 3
ndk:
  path: null   # auto-detect or specify NDK root path
  ollvm_path: null
anti_env:
  enable_root_checks: true
  enable_emulator_checks: true
keys:
  storage_dir: "./keys"
